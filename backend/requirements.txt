# Core web framework
flask>=3.0.0

# CORS support for frontend
flask-cors>=4.0.0

# JWT authentication
flask-jwt-extended>=4.5.0

# ORM for database
flask-sqlalchemy>=3.1.0
sqlalchemy>=2.0.0

# HTTP requests for external APIs (Alpha Vantage, etc.)
requests>=2.31.0
beautifulsoup4>=4.12.0
confluent-kafka>=2.5.0

# WSGI server (recommended for production, optional for dev)
gunicorn>=21.0.0
